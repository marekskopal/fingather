import { PortfolioData } from "@app/_models/portfolioData";
import {GroupWithGroupData} from "@app/_models/groupWithGroupData";

export class Portfolio {
    portfolioData: PortfolioData;
    groups: GroupWithGroupData[];
}
